name: Import govt data (SBSTC + WBTC) and fix railway stations

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  import:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4

      - name: Import SBSTC routes with fares (from committed snapshot)
        run: python3 scripts/import_sbstc.py data/sbstc_routes.json sbstc_buses.json --merge

      - name: Enrich WBTC stoppages
        run: python3 scripts/enrich_wbtc.py

      - name: Attach railway stations
        run: python3 scripts/add_railway_stations.py

      - name: Regenerate SEO pages
        env:
          SITE_BASE: https://busjatri.in
        run: |
          python3 scripts/gen_seo_pages.py
          python3 scripts/build_client_data.py
          python3 scripts/finalize_pages.py

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/busjatri_data.json data/stop_coords.json bus-time-table sitemap.xml robots.txt data/app-index.json data/bus-details.json
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Import SBSTC (routes+fares), enrich WBTC, fix railway stations ($(date -u +%F))"
            git push
          fi
